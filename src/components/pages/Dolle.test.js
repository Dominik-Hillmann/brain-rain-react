// Libraries
import React from 'react';
import * as ReactDOM from 'react-dom';
// Components
import Dolle from "./Dolle";

test('Renders the Dolle component.', () => {
  const root = document.createElement('div');
  <Dolle /> // TODO: not just instantiation because creating client conflicts with tests atm
});
